import type { RuleConfig } from '../rule-config'

/**
 * Order.
 * @see [classnames-order](classnames-order)
 */
export type ClassnamesOrderRuleConfig = RuleConfig<[]>

/**
 * Order.
 * @see [classnames-order](classnames-order)
 */
export interface ClassnamesOrderRule {

    /**
     * Order.
     * @see [classnames-order](classnames-order)
     */
    'unocss/classnames-order': ClassnamesOrderRuleConfig
}
